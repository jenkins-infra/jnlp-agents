
// def dockerImages = ['docker', 'golang']

// dockerImages.each { d ->
//   buildDockerAndPublishImage(d, [mainBranch: 'main'])
// }


buildDockerAndPublishImage('docker', [
  mainBranch: 'main',
  dockerfile: 'docker/Dockerfile',
  registry: 'dduportal',
])
